spring:
  application:
    name: image-processing-ocr
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:your-api-key-here}
      chat:
        options:
          model: gpt-4-turbo
          temperature: 0.7

server:
  port: 8080
  servlet:
    context-path: /

logging:
  level:
    root: INFO
    com.textextraction: DEBUG
    org.springframework.ai: DEBUG
