spring:
  application:
    name: image-processing-ocr
  main:
    web-application-type: none
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:your-api-key-here}
      chat:
        options:
          model: gpt-4-turbo
          temperature: 0.7

logging:
  level:
    root: INFO
    com.textextraction: INFO
